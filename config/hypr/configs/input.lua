hl.config({
    input = {
        kb_layout = "latam",
        -- kb_variant = "colemak",

        numlock_by_default = true,

        follow_mouse = 1,
        sensitivity = 0,
    },
})

hl.config({
    debug = {
        disable_logs = false,
    },
})

